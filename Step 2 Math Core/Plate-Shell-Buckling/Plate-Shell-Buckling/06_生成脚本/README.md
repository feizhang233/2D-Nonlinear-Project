# 生成脚本说明

## 文件来源

- `generate_plate_shell_buckling_pdf.py`：复制自工作区根目录 `scripts/`；
- `generate_shell_instability_research_pdf.py`：复制自工作区根目录 `scripts/`。

两份脚本保留为内容来源和可复现参考。包内副本不应被当作唯一权威代码；若根目录脚本后续修改，应比较差异后再同步，并更新 `AI_CONTENT_INDEX.json` 和来源映射。

## 依赖与运行边界

- 线性屈曲脚本依赖 ReportLab 和 macOS 字体；
- 壳失稳脚本导入线性屈曲脚本中的样式/图形，并依赖 Matplotlib 数学公式渲染；
- 原脚本按自身位置计算输出根目录；移动后运行会改变输出位置；
- 壳失稳脚本原来使用工作区 `tmp/pdfs/` 下的依赖/缓存，未随本知识包复制；
- 在当前包内直接运行前，需检查路径、字体和依赖，不得假定开箱即用；
- 生成/修改 PDF 时应遵循 PDF 工作流：先标记 artifact 操作，生成后渲染全部页面并核对。

## 本次复现状态

- `Plate_Shell_Buckling_完整數學推導與例題_繁中.pdf` 已于 2026-08-29 从根目录原脚本生成；
- `pdfinfo`：19 页，A4，无加密；
- 19/19 页已渲染检查；
- 文本抽取为 21,379 字符，无空白页、无 Unicode 替换字符。

壳失稳 PDF 是工作区已有成品，本次只读检查其元数据为 30 页 A4，并作为第 13–14 周参考纳入。
