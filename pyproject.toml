[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nonlinear-core"
version = "0.1.0"
description = "Contract-first quasi-static nonlinear solution core for 2D finite-element models"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115,<1",
    "gmsh>=4.15,<4.16",
    "numpy>=1.26,<3",
    "matplotlib>=3.9,<4",
    "pydantic>=2.9,<3",
    "scipy>=1.13,<2",
    "uvicorn>=0.30,<1",
]

[project.scripts]
nonlinear-api = "nonlinear_api.main:run"

[project.optional-dependencies]
linear-cores = [
    "continuum-math==0.7.0",
    "frame2d==0.2.0",
    "mindlin-plate-core==0.3.0",
    "shell-core==1.0.0",
]
dev = [
    "build>=1.2",
    "httpx2>=2.12,<3",
    "jsonschema>=4.22,<5",
    "pytest>=8.0",
    "ruff>=0.6",
    "setuptools>=68",
    "wheel",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"reused_cores.frame2d_linear" = ["PROVENANCE.md"]
"reused_cores.frame3d_linear" = ["PROVENANCE.md", "SOURCE_MANIFEST.json"]
"reused_cores.plate3d_linear" = ["PROVENANCE.md", "SOURCE_MANIFEST.json"]
"reused_cores.continuum3d_linear" = ["PROVENANCE.md", "SOURCE_MANIFEST.json"]

"reused_cores.shell3d_linear" = ["PROVENANCE.md", "SOURCE_MANIFEST.json", "data/*.json"]

[tool.pytest.ini_options]
addopts = "-q --strict-markers"
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.lint.per-file-ignores]
# Byte-identical teaching kernel; SOURCE_MANIFEST.json pins its provenance.
"src/reused_cores/continuum3d_linear/reference.py" = ["E501", "E701", "E702", "I001"]

"src/reused_cores/plate3d_linear/reference.py" = ["E501", "E701", "E702", "E741", "I001", "B905"]

# Namespaced frozen shell core; source and packaged hashes are pinned.
"src/reused_cores/shell3d_linear/*.py" = ["E501", "I001"]
