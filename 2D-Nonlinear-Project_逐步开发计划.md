# 2D-Nonlinear-Project 逐步开发计划

## 1. 文档目的

本文把 `2D-Nonlinear-Project` 拆分为可以逐项实施、测试和验收的开发任务。

执行原则：

1. 严格按阶段顺序开发；上一阶段未通过验收，不进入下一阶段。
2. 先固定合同、符号和验证题，再实现求解器和具体单元。
3. 四个完整数学核心作为独立依赖接入；P9 仅将经过来源记录的 Frame 最小复用子集隔离到
   `src/reused_cores`，不得与新非线性算法混放或冒充既有能力。
4. 每次只完成一个边界明确的任务，并同时补充对应测试。
5. 不以“结果看起来合理”代替残量、切线、状态和路径验证。

## 2. 项目目标与首版边界

### 2.1 项目目标

建立一个面向二维有限元模型的通用准静态非线性分析平台，并逐步接入：

- `continuum-math`：二维连续体；
- `frame2d`：二维框架；
- `mindlin-plate-core`：二维板；
- `shell-core`：二维平壳。

平台负责统一的非线性求解、状态事务、控制方法、诊断和结果管理；具体数学核心负责单元运动学、元素内力、切线和结果恢复。

### 2.2 MVP 包含

- 全 Newton-Raphson；
- 可选 modified Newton；
- 荷载控制；
- 位移控制；
- 线搜索；
- 自适应步长和失败回退；
- 球形弧长法；
- trial / commit / rollback 状态管理；
- 完整迭代历史和失败诊断；
- Frame 几何非线性的第一个端到端算例；
- React 建模与结果查看界面；
- JSON 导入、导出和计算重启。

### 2.3 MVP 不包含

- 接触和摩擦；
- 隐式或显式动力；
- 塑性、损伤、黏塑性材料库；
- 裂纹、相场或局部化正则化；
- 自动分岔检测和 branch switching；
- 通用曲壳有限转动；
- 多参数非比例荷载；
- 大型工业模型的并行计算。

## 3. 冻结的数学约定

开发开始后必须保持以下符号链一致：

$$
\mathbf r=\mathbf f_{ext}-\mathbf f_{int}
$$

$$
\mathbf K_t=
\frac{\partial\mathbf f_{int}}{\partial\mathbf u}
-
\frac{\partial\mathbf f_{ext}}{\partial\mathbf u}
$$

$$
\mathbf K_t\,\delta\mathbf u=\mathbf r
$$

统一的模型求值接口为：

```text
evaluate(trial_u, load_factor, committed_state)
    -> internal_force
    -> tangent
    -> trial_state
    -> diagnostics
```

强制规则：

- `internal_force`、`tangent` 和 `trial_state` 必须来自同一个试算点；
- 每次试算都从同一个 `committed_state` 重算；
- 只在全局收敛后执行 `commit()`；
- 被拒绝的迭代或荷载步必须执行 `rollback()`；
- follower load 存在时必须包含外力切线；
- 不得默认所有切线都是对称或正定的。

## 4. 建议项目结构

```text
2D-Nonlinear-Project/
├── pyproject.toml
├── README.md
├── src/
│   ├── reused_cores/
│   │   └── frame2d_linear/     # P9 复用子集与来源记录
│   └── nonlinear_core/
│       ├── contracts.py
│       ├── model.py
│       ├── state.py
│       ├── residual.py
│       ├── convergence.py
│       ├── step_control.py
│       ├── line_search.py
│       ├── arc_length.py
│       ├── linear_solver.py
│       ├── diagnostics.py
│       ├── result.py
│       └── adapters/
│           ├── base.py
│           ├── frame_adapter.py
│           ├── continuum_adapter.py
│           ├── plate_adapter.py
│           └── shell_adapter.py
├── api/
│   ├── main.py
│   ├── schemas.py
│   └── routes/
├── frontend/
├── tests/
│   ├── unit/
│   ├── verification/
│   ├── integration/
│   └── regression/
├── examples/
├── schemas/
├── docs/
└── outputs/
```

## 5. 总体阶段顺序

```text
P0 范围与工程骨架
  ↓
P1 数据合同
  ↓
P2 适配器合同与线性基线
  ↓
P3 残量、约束和线性求解
  ↓
P4 状态事务
  ↓
P5 Newton 与荷载控制
  ↓
P6 位移控制与反力
  ↓
P7 线搜索、减步和诊断
  ↓
P8 弧长法
  ↓
P9 Frame 非线性端到端闭环
  ↓
P10 FastAPI
  ↓
P11 前端工作台
  ↓
P12 Continuum 非线性
  ↓
P13 Plate 非线性
  ↓
P14 Shell 非线性
  ↓
P15 集成验收与发布
  ↓
P16 审阅后集成闭环
```

---

## 6. 逐步开发任务

## P0：范围、环境与工程骨架

**状态：已完成（2026-08-19）**

### P0.1 固定项目范围

- [x] 把 MVP 包含项和排除项写入根目录 `README.md`。
- [x] 明确项目是“准静态非线性求解平台”，不是完整材料、接触或动力程序。
- [x] 明确四个现有核心目前是线性基线。
- [x] 固定 Python 3.11 作为后端运行版本。

### P0.2 创建 Python 工程

- [x] 创建 `pyproject.toml`。
- [x] 创建 `src/nonlinear_core` 包。
- [x] 配置 pytest、Ruff 和包构建。
- [x] 添加最小导入测试。

### P0.3 创建测试和文档目录

- [x] 创建 `tests/unit`。
- [x] 创建 `tests/verification`。
- [x] 创建 `tests/integration`。
- [x] 创建 `examples`、`schemas` 和 `docs`。

### P0 验收门槛

- [x] 包可以安装并成功导入。
- [x] 基础导入测试套件能够执行。
- [x] Ruff、pytest 和包构建命令可运行。
- [x] README 中没有超出 MVP 的能力声明。

---

## P1：统一数据合同

**状态：已完成（2026-08-19）**

### P1.1 定义输入模型

- [x] 定义 `ModelInput`。
- [x] 定义节点、自由度、单元、材料、荷载和约束的公共字段。
- [x] 为不同模型类型保留 `model_family` 和扩展字段。
- [x] 固定单位只作为元数据，不自动换算。

### P1.2 定义分析设置

- [x] 定义 `AnalysisOptions`。
- [x] 包含控制方式、步长、最大迭代数和容差。
- [x] 包含 Newton 类型、线搜索和弧长参数。
- [x] 包含最小/最大步长和 cutback 设置。

### P1.3 定义输出对象

- [x] 定义 `StepResult`。
- [x] 定义 `IterationRecord`。
- [x] 定义 `SolveResult`。
- [x] 定义 `FailureRecord`。
- [x] 定义 `PostResult`。

### P1.4 定义 JSON Schema

- [x] 生成或手写输入 JSON Schema。
- [x] 定义 schema version。
- [x] 为错误位置保留 JSON Path。
- [x] 创建一个最小合法模型和一组非法模型。

### P1 验收门槛

- [x] 合法输入能够稳定解析并重新序列化。
- [x] 未知字段、重复 ID、非法引用和单位缺失能产生结构化错误。
- [x] 同一输入产生确定性的自由度和实体顺序。
- [x] Schema 版本进入求解结果。

---

## P2：适配器合同与四核心线性基线

### P2.1 定义公共适配器

- [x] 定义 `ModelAdapter` Protocol 或抽象基类。
- [x] 定义 `validate()`。
- [x] 定义 `initial_state()`。
- [x] 定义 `evaluate()`。
- [x] 定义 `recover()`。
- [x] 定义自由度映射和约束映射接口。

### P2.2 定义统一响应

- [x] 创建 `ElementResponse` 或 `ModelResponse`。
- [x] 包含 `internal_force`。
- [x] 包含 `tangent`。
- [x] 包含 `trial_state`。
- [x] 包含 `external_force` 和可选外力切线。
- [x] 包含 `detJ`、`detF`、能量和局部失败信息。

### P2.3 接入四个线性核心

- [x] 添加 `frame2d` 线性适配器。
- [x] 添加 `continuum-math` 线性适配器。
- [x] 添加 `mindlin-plate-core` 线性适配器。
- [x] 添加 `shell-core` 线性适配器。
- [x] 只通过包依赖或明确的公共接口调用，不复制源码。

### P2.4 建立线性参考结果

- [x] 每个核心选择一个最小模型。
- [x] 保存位移、反力和能量参考值。
- [x] 确认适配前后结果一致。
- [x] 记录每类核心的自由度顺序。

### P2 验收门槛

- [x] 四个适配器均能返回统一响应对象。
- [x] 四个线性基准与原核心结果一致。
- [x] 适配器不得修改原项目源文件。
- [x] 后续求解器代码不直接判断具体单元类型。

---

## P3：残量、约束与线性方程

### P3.1 实现残量计算

- [x] 实现 `r = f_ext - f_int`。
- [x] 分离自由自由度和约束自由度。
- [x] 保存完整残量和自由自由度残量。
- [x] 添加残量符号单元测试。

### P3.2 实现有效切线

- [x] 实现内力切线减外力切线。
- [x] 记录切线是否对称。
- [x] 不在接口层默认切线正定。
- [x] 添加解析矩阵测试。

### P3.3 实现约束处理

- [x] 支持零位移约束。
- [x] 支持非零给定位移。
- [x] 保存约束自由度用于反力恢复。
- [x] 检查约束不足和刚体模式。

### P3.4 实现线性求解器封装

- [x] 支持稠密小系统作为验证路径。
- [x] 支持 SciPy 稀疏 LU 或等效带主元方法。
- [x] 返回实际线性方程残差。
- [x] 对奇异、病态和非有限结果给出分类错误。

### P3 验收门槛

- [x] V01 线性一步恢复通过。
- [x] 精确切线下一个 Newton 修正到达线性解。
- [x] 约束反力符号正确。
- [x] 线性求解失败不会被报告为非线性收敛。

---

## P4：trial / commit / rollback 状态事务

**状态：已完成（2026-08-19）**

### P4.1 定义状态容器

- [x] 定义不可变 `CommittedState`。
- [x] 定义 `TrialState`。
- [x] 每个状态带 step、iteration 和模型哈希。
- [x] 禁止通过共享可变对象修改 committed state。

### P4.2 实现状态生命周期

- [x] 实现 `begin_step()`。
- [x] 实现从 committed state 重算试算响应。
- [x] 实现 `commit()`。
- [x] 实现 `rollback()`。
- [x] 实现 restart 序列化。

### P4.3 添加污染反例

- [x] 构造会发生失败迭代的路径相关假状态模型。
- [x] 比较直接路径和失败后回退路径。
- [x] 验证两条路径产生相同结果。

### P4 验收门槛

- [x] V07 通过。
- [x] 失败迭代不改变 committed state。
- [x] cutback 后从原提交状态重新开始。
- [x] restart 与连续计算等价。

---

## P5：Newton 与荷载控制

**状态：已完成（2026-08-19）**

### P5.1 实现增量—迭代循环

- [x] 创建荷载步循环。
- [x] 创建迭代循环。
- [x] 每次迭代重新计算响应和残量。
- [x] 全 Newton 每次迭代更新切线。
- [x] modified Newton 只在明确启用时复用切线。

### P5.2 实现荷载控制

- [x] 使用固定参考荷载 `lambda * f_hat`。
- [x] 每一步规定 `delta_lambda`。
- [x] 迭代中仅求解位移修正。
- [x] 保存总荷载因子和步增量。

### P5.3 实现收敛判据

- [x] 实现尺度化残量指标 `eta_R`。
- [x] 实现位移修正指标 `eta_u`。
- [x] 实现能量指标 `eta_E`。
- [x] 检查线性求解残差。
- [x] 检查 NaN、Inf 和元素诊断。

### P5.4 保存迭代历史

- [x] 保存每次残量、修正量和能量指标。
- [x] 保存切线组装次数。
- [x] 保存迭代状态和终止原因。

### P5 验收门槛

- [x] V03 不完美柱 Newton 迭代结果匹配参考答案。
- [x] 全 Newton 在适用区域表现出预期快速收敛。
- [x] modified Newton 不被错误要求二次收敛。
- [x] 极限点附近失败能够分类为控制或切线问题。

---

## P6：位移控制与反力

**状态：已完成（2026-08-19）**

### P6.1 实现位移控制输入

- [x] 定义控制自由度。
- [x] 定义每一步目标位移增量。
- [x] 验证控制自由度存在且未冲突。

### P6.2 实现分块求解

- [x] 建立自由/给定位移分块系统。
- [x] 把给定位移修正移到右端项。
- [x] 求解剩余自由位移修正。

### P6.3 恢复荷载因子与反力

- [x] 保存位移控制下的荷载因子或反力响应。
- [x] 从完整平衡方程恢复反力。
- [x] 分离约束反力和自由残量。

### P6 验收门槛

- [x] V05 的位移修正和反力参考值通过。
- [x] V04 中位移控制可以越过荷载极限点。
- [x] 控制位移反向时能够报告控制方式失效。

---

## P7：线搜索、自动减步和失败诊断

**状态：已完成（2026-08-19）**

### P7.1 实现线搜索

- [x] 支持 Newton 全步 `alpha=1`。
- [x] 支持回溯线搜索。
- [x] 支持基于正交条件的线搜索检查。
- [x] 对非保守系统使用明确标记的残量 merit function。

### P7.2 实现步长调整

- [x] 定义目标迭代次数。
- [x] 实现收敛快时放大步长。
- [x] 实现失败时 cutback。
- [x] 设置最小和最大步长。
- [x] 设置最大重试次数。

### P7.3 实现失败分类

- [x] `MODEL_ERROR`：模型或几何错误。
- [x] `CONTROL_ERROR`：控制方式无法表示路径。
- [x] `TANGENT_ERROR`：切线方向差分失败。
- [x] `STATE_ERROR`：状态污染或回滚失败。
- [x] `LINEAR_SOLVE_ERROR`：线性系统失败。
- [x] `LOCAL_MATERIAL_ERROR`：材料点积分失败。
- [x] `NONCONVERGENCE`：超过最大迭代数。

### P7 验收门槛

- [x] V06 线搜索正交测试通过。
- [x] 失败步能够减步重试。
- [x] 达到最小步长后明确终止。
- [x] 所有被拒绝的步都保留失败证据。

---

## P8：球形弧长法

**状态：已完成（2026-08-19）**

### P8.1 定义弧长控制参数

- [x] 定义弧长 `delta_l`。
- [x] 定义荷载缩放参数 `beta`。
- [x] 保存前一步增量方向。

### P8.2 实现预测器

- [x] 计算切线位移方向。
- [x] 使用弧长约束缩放预测增量。
- [x] 根据前一步方向选择预测符号。

### P8.3 实现校正器

- [x] 同时满足平衡方程和弧长约束。
- [x] 复用相同切线求解两个右端项。
- [x] 计算位移修正和荷载因子修正。
- [x] 两个校正右端项共用一次平衡、诊断和矩阵分解，并分别核验原方程残量。

### P8.4 实现根选择与失败处理

- [x] 优先选择与历史增量方向连续的根。
- [x] 检测复根和强曲率。
- [x] 失败时缩小弧长并重试。
- [x] 保存所有候选根和选择理由。
- [x] 根判别与方向连续性采用实际量级的相对容差，避免小量纲误判。
- [x] 重启前核验历史增量半径和约束自由度，并预检固定比例荷载边界。
- [x] 步基线异常返回结构化拒绝记录并保留最后提交态，不向调用方泄漏。

### P8 验收门槛

- [x] V08 一自由度弧长题通过。
- [x] 平衡残量和弧长约束同时收敛。
- [x] V04 极限点之后仍能继续跟踪路径。
- [x] snap-back 对照中弧长法可以继续而单一位移控制按预期失败。
- [x] 极小量纲根、复根、非法重启、非比例荷载和最大重试数反例通过。
- [x] 多右端项单次分解、步基线异常和二次系数溢出反例通过。

---

## P9：Frame 几何非线性端到端闭环

**状态：已完成（2026-08-19）**

### P9.1 确定单元形式

- [x] 采用二维共回转 Euler-Bernoulli 框架单元。
- [x] 明确大转动、小应变边界。
- [x] 固定局部/全局自由度和转角符号。

### P9.2 实现非线性元素响应

- [x] 将 `frame2d` 的可复用节点、单元、荷载、参考几何和线性符号子集隔离到
  `src/reused_cores/frame2d_linear`，记录来源 commit 和 SHA-256；结果继续使用公共合同。
- [x] 实现当前构形下的局部坐标系。
- [x] 实现元素内力。
- [x] 实现材料刚度和几何刚度。
- [x] 实现一致切线。

### P9.3 实现 Frame 结果恢复

- [x] 恢复节点位移和反力。
- [x] 恢复轴力、剪力和弯矩。
- [x] 输出荷载—位移曲线。
- [x] 标记当前构形和参考构形结果。

### P9.4 建立验证算例

- [x] 刚体运动零内能。
- [x] 小位移结果退化到原线性 `frame2d`。
- [x] 元素和装配级方向差分。
- [x] 不完美柱 Newton 回归。
- [x] 保留 V04 刚杆—弹簧控制边界，并增加真实 Frame 浅拱 snap-through 路径。
- [x] 三档步长敏感性。

### P9 验收门槛

- [x] Frame V09 集成证据通过。
- [x] 方向差分出现合理误差谷值。
- [x] 稳定段上三种控制方法结果一致。
- [x] 极限点和 snap-back 的预期失败对照正确。

---

## P10：FastAPI 服务

**状态：已完成（2026-08-19）**

### P10.1 创建基础服务

- [x] 创建 `/health`。
- [x] 创建 `/api/v1/models/validate`。
- [x] 创建 `/api/v1/analyses`。
- [x] 创建 `/api/v1/analyses/{id}`。

### P10.2 定义运行模式

- [x] 小模型同步计算。
- [x] 支持进程内异步任务、GET 轮询和真实步/迭代进度。
- [x] 添加请求大小和自由度限制。
- [x] 支持客户端协作取消，并防止迟到结果覆盖取消记录。
- [x] API 接入自适应荷载/位移控制，而不是忽略 cutback 设置。
- [x] 接受经过模型哈希和适配器校验的重启状态及弧长历史增量。

### P10.3 定义 API 错误合同

- [x] 区分输入错误、计算失败和服务器错误。
- [x] 返回结构化诊断代码和位置。
- [x] 失败分析不生成伪造结果字段。

### P10 验收门槛

- [x] OpenAPI Schema 可生成。
- [x] 合法 Frame 示例能够通过 API 完成分析。
- [x] 非法模型返回稳定的 4xx 错误。
- [x] 非收敛返回分析失败记录，而不是 500。

---

## P11：前端 Nonlinear Studio

**状态：已完成（2026-08-19）**

### P11.1 创建工作台骨架

- [x] 使用 React、TypeScript、MUI 和 Vite。
- [x] 创建模型区、属性区、分析设置区和结果区。
- [x] 建立统一状态管理。

### P11.2 实现模型输入

- [x] 首先支持 Frame 节点和单元编辑。
- [x] 支持材料、截面、支座和荷载。
- [x] 支持 JSON 导入和导出。
- [x] 支持版本化计算重启包导入和导出。
- [x] 模型变化后自动使旧结果失效。

### P11.3 实现非线性设置

- [x] 选择荷载、位移或弧长控制。
- [x] 设置步长、容差和最大迭代数。
- [x] 设置线搜索和 cutback。
- [x] 对不适用选项给出说明。

### P11.4 实现计算监视器

- [x] 显示当前荷载步和迭代次数。
- [x] 显示残量、修正和能量指标。
- [x] 显示接受步、拒绝步和回滚。
- [x] 显示失败原因和建议检查项。

### P11.5 实现结果显示

- [x] 变形图。
- [x] 反力和内力图。
- [x] 荷载—位移曲线。
- [x] 收敛历史曲线。
- [x] 步结果表和失败步表。

### P11 验收门槛

- [x] 用户可以从建模到结果完成一个 Frame 非线性算例。
- [x] 修改模型后不能继续显示旧结果为当前结果。
- [x] 失败分析能够在界面查看完整原因。
- [x] UI 不把弧长收敛解释为稳定性证明。

---

## P12：Continuum 非线性扩展

**状态：已完成（2026-08-20）**

### P12.1 固定首个连续体模型

- [x] 首版选择 Total Lagrangian Q4。
- [x] 首版只实现一种经过验证的弹性有限应变材料。
- [x] 固定 plane strain 或明确处理 plane stress 厚度更新。

### P12.2 实现连续体响应

- [x] 计算变形梯度 `F` 和 `detF`。
- [x] 实现功共轭应力和应变。
- [x] 实现元素内力。
- [x] 实现材料和几何切线。
- [x] 拒绝 `detF <= 0`。

### P12.3 验证与后处理

- [x] V00 刚体转动客观性。
- [x] 元素和装配级 V02。
- [x] 小应变极限与 `continuum-math` 对照。
- [x] 网格和步长敏感性。
- [x] 原始积分点应力保留。

### P12 验收门槛

- [x] Continuum V09 通过。
- [x] 刚体转动不产生伪应力和伪能量。
- [x] 所有接受步满足 `detF > 0`。
- [x] 节点平滑应力明确标记为派生结果。

---

## P13：Plate 非线性扩展

**状态：已完成（2026-08-20）**

### P13.1 固定首个板模型

- [x] 首版采用 von Kármán 几何非线性。
- [x] 明确适用于中等转动而非任意有限转动。
- [x] 选择 DKQ 或 MITC4 的首个实现路径。

### P13.2 实现板响应

- [x] 增加膜—弯曲耦合所需自由度或合同扩展。
- [x] 实现非线性应变项。
- [x] 实现内力和一致切线。
- [x] 复用现有板积分和结果恢复能力。

### P13.3 验证

- [x] 小位移极限与 `mindlin-plate-core` 对照。
- [x] 方向差分。
- [x] 薄厚板和锁死敏感性。
- [x] 步长与网格收敛。

### P13 验收门槛

- [x] Plate V09 通过。
- [x] 膜、弯曲和剪切能量可分别核对。
- [x] 原始积分点结果不被节点平均覆盖。

---

## P14：Shell 非线性扩展

**状态：已完成（2026-08-20）**

### P14.1 固定首个壳模型

- [x] 首版采用共回转平壳路线。
- [x] 明确大刚体转动、小局部应变边界。
- [x] 不宣称通用曲壳或完整有限转动壳。

### P14.2 实现壳适配

- [x] 复用 `shell-core` 的 Q4 局部线性算子。
- [x] 更新当前构形局部坐标系。
- [x] 映射局部内力和切线到全局。
- [x] 处理 drilling stabilization 的转动一致性。
- [x] 保留原始 Gauss 点 `N/M/Q`。

### P14.3 验证

- [x] 大刚体转动零内能。
- [x] 小转动极限与 `shell-core` 对照。
- [x] 变换前后虚功一致。
- [x] 方向差分和稳定参数敏感性。
- [x] 网格畸变和厚度敏感性。

### P14 验收门槛

- [x] Shell V09 通过。
- [x] 坐标变换满足功一致性。
- [x] drilling 参数变化不会被隐藏。
- [x] 不把单个平壳算例通过解释为通用非线性壳完成。

---

## P15：集成验收与发布

**状态：已完成（2026-08-20，`nonlinear-core 0.1.0`）**

### P15.1 完整自动化测试

- [x] V00–V08 全部通过。
- [x] 四个适配器分别通过 V09。
- [x] 四个原线性核心的参考结果未退化。
- [x] API 和前端端到端测试通过。

### P15.2 数值交叉验证

- [x] 稳定路径上三种控制方式重合。
- [x] 极限点保留荷载控制预期失败对照。
- [x] snap-back 保留位移控制预期失败对照。
- [x] 至少使用三档步长。
- [x] 至少使用两档或三档网格。

### P15.3 发布检查

- [x] Ruff、pytest、类型检查和前端构建通过。
- [x] 创建干净环境安装包并运行示例。
- [x] Python 3.11/3.12 CI 矩阵执行完整后端门禁。
- [x] 无 `system-site-packages` 的 wheel 环境解析依赖并执行 `pip check`。
- [x] 真实启动 API 与 Vite，通过前端代理完成异步 HTTP 冒烟。
- [x] 检查 Schema、包版本和运行时版本一致。
- [x] README 仅记录已验证能力。
- [x] 发布示例包含输入、结果、迭代历史和失败证据。

### P15 最终完成门槛

- [x] Frame、Continuum、Plate 和 Shell 的支持范围分别说明。
- [x] 所有成功结果可以追溯到模型、算法和版本。
- [x] 所有失败结果保留步长、迭代和回滚记录。
- [x] 未实现功能在 README 和 UI 中明确标识。
- [x] 不存在用更松容差掩盖切线或状态错误的测试。

---

## P16：审阅后集成闭环

**状态：已完成（2026-08-20）**

### P16.1 合同和运行闭环

- [x] 核心求解器向外报告真实 Step、Iteration 和接受步计数。
- [x] API 和前端贯通异步轮询、协作取消及可验证重启。
- [x] 弧长重启同时保留提交状态和上一收敛增量，拒绝不完整继续条件。
- [x] 命令行模块入口可直接启动，并提供显式 CORS 配置。

### P16.2 发布和文档闭环

- [x] CI 覆盖 Python 3.11 与 3.12，并在当前环境及干净 wheel 环境执行 `pip check`。
- [x] wheel 安装不再借用系统 site-packages；四类公开算例在安装环境中冒烟。
- [x] sdist 明确包含计划、技术文档、Schema、示例、脚本和前端源码。
- [x] Vite 生产包拆分，消除单 chunk 超过默认阈值的警告。
- [x] 完整后端、前端、构建、sdist/wheel 和本地全栈回归全部通过。

---

## P17：四类前端闭环

**状态：已完成（2026-08-20）**

### P17.1 类型合同与示例

- [x] 前端 `ModelInput` 接受 Frame、Continuum、Plate 和 Shell，不再把
  `model_family` 固定为 Frame。
- [x] 四类均提供完整可运行的验证示例、默认步长/目标和家族能力说明。
- [x] JSON 模型与重启包导入保留家族合同，并按该家族补齐缺省分析字段。

### P17.2 建模与分析

- [x] 模型浏览器按家族创建 2 节点 Frame 或 4 节点 Q4 单元。
- [x] 属性面板按家族编辑坐标、材料参数、单元参数、约束及节点荷载。
- [x] 位移控制自由度、默认控制点和监测自由度使用实际家族 DOF 顺序。
- [x] 四类均通过同一异步验证、提交、轮询、取消及重启链路。

### P17.3 可视化与恢复结果

- [x] Frame 使用线单元视图；Continuum、Plate、Shell 使用 Q4 面投影。
- [x] Plate/Shell 的 `UZ` 使用带明确说明的斜向工程投影，不冒充通用三维视图。
- [x] 结果表分别显示 Frame 端力、Continuum Cauchy 应力及 Plate/Shell `N/M/Q`。
- [x] 四类模型切换会清除旧结果和重启状态，避免跨家族结果污染。

### P17 验收门槛

- [x] 四类模型可在浏览器内完成选择、校验、异步求解和结果查看。
- [x] 每个家族提交的 `model_family`、单元公式和 DOF 与界面选择一致。
- [x] 类型检查、前端测试、生产构建、四类真实 HTTP 冒烟和浏览器检查通过。

## 7. 验证任务对应表

| 验证 ID | 内容 | 首次执行阶段 | 后续回归阶段 |
|---|---|---:|---|
| V00 | 二维刚体转动客观性 | P9 | P9、P12–P15 |
| V01 | 残量符号和线性一步恢复 | P3 | P3–P15 |
| V02 | 一致切线方向差分 | P3 | P3–P15 |
| V03 | 不完美柱 Newton 路径 | P5 | P5–P15 |
| V04 | 极限点和控制边界 | P6 | P6–P15 |
| V05 | 位移控制和反力 | P6 | P6–P15 |
| V06 | 线搜索正交 | P7 | P7–P15 |
| V07 | trial / commit / rollback | P4 | P4–P15 |
| V08 | 球形弧长和根选择 | P8 | P8–P15 |
| V09 | 具体二维模型集成 | P9 | P9、P12–P15 |

## 8. 每个开发任务的完成定义

一个任务只有同时满足以下条件，才可以勾选为完成：

1. 实现代码已经提交到明确文件；
2. 对应单元测试已经添加；
3. 至少一个错误或反例测试已经添加；
4. 相关验证题已经执行；
5. 输出包含足够的诊断和失败证据；
6. 文档描述与实际能力一致；
7. 没有修改任务范围之外的数学约定；
8. 上一阶段的回归测试仍然通过。

## 9. 推荐的实际启动顺序

第一次正式开发时，按下面顺序启动：

1. 完成 P0，建立空工程和测试命令；
2. 完成 P1，只定义数据对象和 JSON Schema；
3. 完成 P2，只做四个线性核心适配和参考结果；
4. 完成 P3–P8，形成不依赖具体单元的非线性求解器；
5. 完成 P9，用 Frame 打通第一个真实非线性闭环；
6. 完成 P10–P11，建立可使用的 Web MVP；
7. 按 P12、P13、P14 的顺序扩展 Continuum、Plate 和 Shell；
8. 最后执行 P15 的完整验收和发布。
9. 发布前执行 P16 审阅闭环，核对计划勾选、跨层接线和隔离环境证据；
10. 执行 P17 四类前端闭环，逐类核对模型合同、画布投影和恢复结果。

在 P9 完成之前，不建议同时开发四类非线性单元；否则求解器错误、单元错误和状态错误会互相混杂，难以独立定位。
