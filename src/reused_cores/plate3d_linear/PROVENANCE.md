# MITC4 reference kernel

Byte-identical snapshot of `docs/3D-Plate_Math-Core-Guide/07_可复现验证/reference_plate.py`. SHA-256 is pinned in SOURCE_MANIFEST.json. The original teaching package remains unchanged. The host owns schema validation, resource limits, assembly, scaled solve, and result checks. Reference validation is distinct from product validation.
