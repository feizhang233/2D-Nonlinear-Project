# Regression tests

Frozen numerical references live beside the feature that owns them: P2 core baselines are in
`examples/adapters/reference-results.json`, nonlinear V09 paths are in `tests/verification`, and
P15 API success/failure serialization is in `examples/p15`. This directory remains reserved for
future baselines that do not have a clearer owning feature.
