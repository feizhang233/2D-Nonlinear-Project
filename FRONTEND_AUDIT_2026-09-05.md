# 前端交互、画布与接口修复记录

本次检查基于当前源码、既有工作区契约、组件测试和本地浏览器。保留并整合了仓库已有的 Math Core 接入；以下记录实现与本地验收结果，不代表线上部署。

## 交互与数据流

| 用户操作 | 数据流与提交边界 |
| --- | --- |
| 选择模型族 | `App` → `studioReducer` → 对应 `WorkspaceState`，恢复独立模型、选择、参数及结果 |
| 表单、几何编辑 | `PropertyPanel` / `GeometryPanel` / `ModelCanvas` → 草稿；Apply 后增加 revision 并使旧结果失效 |
| 生成网格 | `/api/v1/meshes` → `applySurfaceMesh` → 重绑边界、支座和荷载 → 草稿 → Apply |
| 求解 | 校验模型 → 检查 `execution_eligible` → POST 异步任务 → GET 轮询 → terminal record → Results |
| 取消求解 | 已有任务 ID 时发 DELETE 并继续监控；提交尚未返回时保留其应答以取消迟到的任务 |
| 私有历史 | `useModelHistory` → 带 HttpOnly 会话的 API → 带 owner 和版本校验的界面更新 |
| 导入 | 解析和补齐兼容字段 → 后端模型校验 → 草稿离开保护 → 替换工作区 |

## 已修复的问题

| 优先级 | 问题与影响 | 修复 |
| --- | --- | --- |
| P1 | SVG 留白未纳入坐标换算，点击/拖动位置偏移；拖动时自动适配造成跳动 | 使用 SVG screen CTM 和留白正确的后备换算；拖动冻结相机，预览后在松开时一次写入草稿 |
| P1 | 在 POST 返回前取消可能留下后台求解任务 | 接收提交应答后取消对应任务；轮询和终态更新都校验当前请求 |
| P1 | 取消失败被吞掉，界面提前显示停止 | 显示失败并允许重试；已有任务继续轮询，直到后端确认终态 |
| P1 | 后端校验/连接失败后，Failures 页面可能显示“没有失败记录” | 错误在所属结果面板持续显示；无结果时使用明确的空状态 |
| P1 | 导入浅层校验允许损坏的节点/单元结构进入渲染 | 替换前执行后端 schema 校验；失败保留当前模型 |
| P1 | 同节点多荷载/多分量只显示最后一项或首个分量；网格非轮廓节点反力被过滤 | 显示所有非零荷载分量，旋转分量用弯矩弧；显示所有受约束网格节点反力 |
| P1 | SPA 静态文件路径可越出构建目录 | 解析真实路径并验证目录包含关系，含符号链接；加入回归测试 |
| P2 | 取消后同 revision 的迟到响应仍可能更新状态，非活动工作区完成会改变模式 | reducer 校验 running 状态；只让活动工作区终态改变可见模式 |
| P2 | 全局 Ctrl/Command + Enter 可在输入、IME、弹窗里误触发，快捷键未统一阻止网格忙时求解 | 统一快捷键保护、同步请求锁与忙状态检查 |
| P2 | 轮询每次增加 abort listener；请求可长期无响应；HTML 200 被当作 JSON 成功 | 清理监听器；30 秒请求超时、120 秒网格超时；区分无效响应、离线、主动取消和超时 |
| P2 | 超出 DOF 限制只显示通用失败，schema 错误缺少字段提示 | 使用后端 `limit_error`、字段路径和具体错误信息 |
| P2 | 早发的历史列表响应覆盖新保存记录；退出账户时忙状态残留 | 历史更新加 mutation version，保留 owner 校验并清理忙状态 |
| P2 | 模型树和宽属性面板挤占画布；顶部流程条过高 | 240 px 左侧导航、中央弹性画布、312 px 右侧属性；紧凑顶部与全宽事务栏 |
| P2 | Results 沿用旧底部 dock 的 118/216 px 限高及横排双图 | 独立结果页扩大表格，纵排曲线和表格；SVG 图表按比例使用完整宽度 |
| P2 | 密网格文字拥挤；几何点与网格点标签重合；结果页覆盖可编辑轮廓 | 收敛密网格标签、错开几何标签、结果模式隐藏编辑轮廓；保留节点选择和只读提示 |
| P2 | Fit 为禁用按钮、无法平移/缩放；焦点样式被移除 | 实际 Fit、滚轮/按钮缩放、拖动/方向键平移、Escape 取消、可见焦点和图层实例 ID |
| P2 | 非有限曲线样本进入 SVG path | 在无效样本处断开线段，避免 NaN 坐标和虚构插值 |
| P2 | 小屏横向内容被隐藏；下拉菜单可能宽于字段 | 保留 1120 px 桌面支持下限并允许文档横向滚动；共享 MUI Select 菜单约束与换行 |

## 视觉系统与契约同步

| 原设计规则 | 本次选择 | 实现与文档 |
| --- | --- | --- |
| 白色顶栏、钴蓝按钮、圆胶囊控件 | 深石油色顶栏、青绿动作色、紧凑矩形控件 | `theme.ts` 与 `DESIGN.md` 同步；已有工程几何/荷载符号颜色保留语义 |
| 左侧树和表单占据主要面积 | 中央模型为主、右侧检查属性 | `App.tsx` 与 `UX-CONTRACT.md` 同步 |
| 自动缩放视口、不可用 Fit | 物理坐标网格与可操纵相机 | `canvasViewport.ts`、`ModelCanvas.tsx` |
| 独立结果页沿用 dock 密度 | 按独立面板空间排布 | `ResultsDock.tsx`、`MiniChart.tsx` |

视觉值仍以 `frontend/src/theme.ts` 为运行时来源，文档记录已接受的值和使用理由。英文界面、四工作区、模型/结果分离、表面网格只读、Math Core 独立工具边界保持一致。

## 验证

- 前端：**69 tests passed / 16 files**，TypeScript 和生产构建通过；回归覆盖坐标变换、拖动事务、取消竞态、导入失败、请求超时、历史竞态和错误反馈。
- 后端：全量 pytest **209 passed**；Ruff 与 `git diff --check` 通过。
- 严格 UI 静态审计：0 findings。官方 DESIGN.md lint：0 errors；9 个未在 YAML component 字段引用的 token 警告，运行时映射已在文档中说明。
- 真实 HTTP：通过 Vite 代理完成 Frame / Continuum / Plate / Shell 校验、提交、轮询和恢复量检查，全部 succeeded；分别 9 / 8 / 20 / 24 DOF。
- 浏览器：Frame 求解进入 Results；路径/收敛曲线；Gmsh 生成 15 节点、8 Q4；只读网格节点；应用草稿并切换工作区；鼠标拖动；下拉展开和 Escape；1120 px 布局；900 px 窄窗口横向可达性。
- 最终生产构建直接由更新后的 FastAPI 提供，浏览器完成 4 个 accepted steps；该次加载和结果切换没有新增控制台 error/warn。
- 下拉几何实测：字段 137.5 px、菜单 138 px，宽度差 0.5 px。

## 边界

仍是桌面工程工作台，1120 px 以下采用横向访问；没有宣称移动端专用布局。表面/壳的二维投影、求解器规模和物理适用范围不因 UI 改版扩大。网络断开后的非幂等请求可能已在服务器执行，界面明确报告完成状态未知，不自动重发。没有进行全浏览器矩阵、屏幕阅读器审计或 10,000 节点性能基准。
